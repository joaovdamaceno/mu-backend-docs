---
title: API (visão geral)
---

A API é REST e normalmente roda em:

- `http://localhost:8080`

O padrão do projeto é:

- Endpoints base em `/api/...`
- JSON como request/response
- IDs numéricos (`Long` / `BIGSERIAL`)

Veja a lista completa em **API → Endpoints**.
